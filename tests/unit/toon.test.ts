import { describe, it } from 'vitest';

describe('encodeToon', () => {
  it.todo('returns a string, not JSON-parseable as identical object');
  it.todo('output contains "ok: true" for success envelope');
  it.todo('array data uses tabular format with tab delimiters');
  it.todo('handles null values without error');
  it.todo('handles empty arrays without error');
  it.todo('error envelope encodes correctly (ok: false, error present)');
});
